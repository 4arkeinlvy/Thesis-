# IDE settings
.vscode/

# OS / env junk
.DS_Store
.venv/

# Scratch logs & temp output
*.error
erroronnx.log
hf_upload.log
retry.txt

# Reference PDFs of other people's theses (large, not ours)
/18221100*.pdf
/skripsi*.pdf

# Secrets / credentials — never commit
SupabaseCreds.txt
*.env
.env.*
credentials.json
token.json

# Privacy: raw questionnaire data/screenshots with respondent names (PII) — never commit
Docs/Hasil Kuesioner /
*kuesioner-nama*
*Hasil-kuesioner-nama*

# Scraper output (large)
smartCityReport/artifacts/crm_jakarta/
# Flutter dart-define JSON files (keep .example.json committed)
smartCityReport/smart_city_reporter_app/env/*.json
!smartCityReport/smart_city_reporter_app/env/*.example.json

# LaTeX auxiliary files
*.log
*.tex.swp
*.bbl
*.aux
*.synctex.gz
*.fdb_latexmk
*.fls
*.out
*.toc

# Build artifacts
*.app
# Pasted PAT scratch file
token.txt

# pgs / multi-instansi analysis sources kept local only — not published
Docs/chapters/pgs_analysis.tex
Docs/chapters/multi_instansi_handling.tex
Docs/standalone_builds/pgs_driver.tex
Docs/standalone_builds/multi_instansi_driver.tex

# AI assistant guidance file kept local only
CLAUDE.md
